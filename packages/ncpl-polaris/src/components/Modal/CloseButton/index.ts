import { withInstall } from '../../../utils'
import CloseButton from './CloseButton.vue'

export const NpModalCloseButton = withInstall(CloseButton)
export default NpModalCloseButton
export * from './CloseButton'
